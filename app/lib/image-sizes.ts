/**
 * Default `sizes` for full-width column images.
 * Mobile (<640px): `calc(100vw - 3rem)` per layout spec; wider breakpoints approximate the 42rem column.
 */
export const SITE_IMAGE_SIZES_FULL_WIDTH =
  '(max-width:639px) calc(100vw - 3rem), (max-width:1280px) min(100vw, 42rem), 42rem'
